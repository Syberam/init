#permanently modify system name by editing file:
/etc/hostname

# to be sure to not have any error with sudo command check the file /etc/hosts and verify if local domain name correspond to the new system name
